void main() {
  // named parameter
  sum();
  // required parameter
  sum2(x: 10, y: 20, z: 30);
}

// named parameter
sum({int x = 10, int y = 15, int z = 30}) {
  print(x + y + z);
}

// required parameter
sum2({required int x, required int y, required z}) {
  print(x + y + z);
}
